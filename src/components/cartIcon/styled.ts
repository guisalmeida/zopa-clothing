import styled from 'styled-components';

export const CartIconCounter = styled.sup`
    overflow: hidden;
    position: absolute;
    right: -1rem;
    top: -0.5rem;
    height: 1.7rem;
    border-radius: 50%;
    width: 1.7rem;
    background: #cf3838;
    color: #fff;
    line-height: 1.7rem;
    text-align: center;
    padding: 0;
    font-size: 1.1rem;
    font-weight: 700;
    white-space: nowrap;
    z-index: 10;
`
